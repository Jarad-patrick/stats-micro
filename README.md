# stats-micro

Python utilities for basic statistics: `mean`, `median`, `mode`.

## Usage

```python
from stats_pkg.basics import mean, median, mode

print(mean([1, 2, 3]))      # 2.0
print(median([3, 1, 2]))    # 2.0
print(mode([1, 1, 2, 3]))   # [1]
